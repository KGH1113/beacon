{
  "name": "@beacon/db",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "db:generate": "prisma generate",
    "db:migrate:dev": "prisma migrate dev",
    "db:migrate:deploy": "prisma migrate deploy || bun run scripts/migrate-sqlite.ts",
    "build": "bun run db:generate && tsc --noEmit -p tsconfig.json",
    "lint": "biome check .",
    "typecheck": "bun run db:generate && tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@beacon/shared": "workspace:*",
    "@prisma/client": "^6.7.0"
  },
  "devDependencies": {
    "prisma": "^6.7.0"
  }
}
